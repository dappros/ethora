import { ModelChat } from "../../models"

interface Props {
    chatId: string,
    chat: ModelChat,
}

export default function ChatInput(props: Props) {
    return (
        <div>ChatInput</div>
    )
}
