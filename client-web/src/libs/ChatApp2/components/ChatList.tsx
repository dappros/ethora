import { ModelChat } from "../models"

interface Props {
    chatList: Array<ModelChat>,
    chatId: string
}

export default function ChatList(props: Props) {
    return (
        <div>ChatList</div>
    )
}